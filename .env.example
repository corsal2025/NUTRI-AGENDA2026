# Variables para la app Expo (raiz del repo)
# 1) Copia a `.env`
# 2) Solo las que empiezan con EXPO_PUBLIC_ llegan al codigo.
#
# Web Next.js: usar `web/.env.local` (ver `web/.env.example`)
# Backend FastAPI: usar `backend/.env` (ver `backend/.env.example`)

# Firebase (Expo)
EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=

# Mercado Pago (solo public key en cliente)
EXPO_PUBLIC_MERCADOPAGO_PUBLIC_KEY=

# App
APP_ENV=development
